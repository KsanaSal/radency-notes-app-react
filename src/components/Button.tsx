const Button = () => {
    return (
        <button
            className="text-gray-800 font-bold text-[18px] border-2 px-3 py-1 rounded-lg border-teal-800 bg-teal-200 hover:bg-teal-800 hover:text-white block mt-4 ml-auto"
            data-create-note
        >
            Create Note
        </button>
    );
};

export default Button;

import ArchivedTable from "../components/archivedTable/ArchivedTable";

const ArchivedNotes = () => {
    return (
        <section className="text-gray-800 w-full">
            <h1 className="text-[40px] font-bold">Archived list</h1>
            <ArchivedTable />
        </section>
    );
};

export default ArchivedNotes;
